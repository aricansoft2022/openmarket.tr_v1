# Used only by Drizzle Kit, verification and administrative scripts.
# Runtime Worker traffic uses Hyperdrive instead of DATABASE_URL.
DATABASE_URL=postgresql://openmarket:openmarket@127.0.0.1:5432/openmarket

# When a Neon development database is provisioned, replace the local URL with
# its direct (non-Hyperdrive) migration URL and retain the required SSL options.
