name: Prepare audited supplier migration

on:
  push:
    branches: [agent/supplier-company-foundation]

permissions:
  contents: write

concurrency:
  group: prepare-audited-supplier-migration
  cancel-in-progress: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/supplier-company-foundation
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Generate reviewed migration
        run: npm run db:generate

      - name: Format repository
        run: npm run format

      - name: Commit prepared files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "fix(supplier): regenerate audited company migration"
          git push origin HEAD:agent/supplier-company-foundation
