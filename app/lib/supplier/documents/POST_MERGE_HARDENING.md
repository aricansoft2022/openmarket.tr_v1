# Supplier document post-merge hardening

This temporary marker triggers the existing pull-request materialization workflow so the prepared source can be verified and committed as normal repository files. It must be removed before merge.
